STEPS

1. Abra o terminal dentro da pasta rabbit;

2. Execute o comando <docker build -t rabbitmq-local .>

3. Execute o comando <docker run -d --name rabbitmq --hostname rabbitmq rabbitmq-local>

3. Abra no navegador: <localhost:15674>

4. Entre com o login:
    Username: dev
    Password: 12345678